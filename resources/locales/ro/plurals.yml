---
:ro: '{ :''ro'' => { :i18n => { :plural => { :keys => [:one, :few], :rule => lambda
  { |n| n = n.respond_to?(:abs) ? n.abs : 0;n.to_f == 1 ? :one : (n.to_f == 0 || (n.to_f
  != 1 && (((n.to_f % 100) % 1).zero? && (1..19).include?(n.to_f % 100)))) ? :few
  : :other } } } } }'
