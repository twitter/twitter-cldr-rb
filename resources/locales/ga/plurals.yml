---
:ga: '{ :''ga'' => { :i18n => { :plural => { :keys => [:one, :two, :few, :many, :other],
  :rule => lambda { |n| n = n.respond_to?(:abs) ? n.abs : ((m = n.to_s)[0] == "-"
  ? m[1,m.length] : m); n.to_f == 1 ? :one : n.to_f == 2 ? :two : ((n.to_f % 1).zero?
  && (3..6).include?(n.to_f)) ? :few : ((n.to_f % 1).zero? && (7..10).include?(n.to_f))
  ? :many : :other } } } } }'
