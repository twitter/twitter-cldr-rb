---
:ga: ! '{ :''ga'' => { :i18n => { :plural => { :keys => [:one, :two, :few, :many,
  :other], :rule => lambda { |n| n.to_f.abs == 1 ? :one : n.to_f.abs == 2 ? :two :
  ((n.to_f.abs % 1).zero? && (3..6).include?(n.to_f.abs)) ? :few : ((n.to_f.abs %
  1).zero? && (7..10).include?(n.to_f.abs)) ? :many : :other } } } } }'
