---
:cy: '{ :''cy'' => { :i18n => { :plural => { :keys => [:zero, :one, :two, :few, :many,
  :other], :rule => lambda { |n| n.to_f == 0 ? :zero : n.to_f == 1 ? :one : n.to_f
  == 2 ? :two : n.to_f == 3 ? :few : n.to_f == 6 ? :many : :other } } } } }'
