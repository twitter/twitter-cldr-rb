---
:lv: '{ :''lv'' => { :i18n => { :plural => { :keys => [:zero, :one], :rule => lambda
  { |n| n = n.respond_to?(:abs) ? n.abs : 0;n.to_f == 0 ? :zero : (n.to_f % 10 ==
  1 && n.to_f % 100 != 11) ? :one : :other } } } } }'
