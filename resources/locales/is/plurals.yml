---
:is: '{ :''is'' => { :i18n => { :plural => { :keys => [:one, :other], :rule => lambda
  { |n| ((((t = n.to_s.split(".")).count > 1 ? t.last.gsub(/0+$/, "").to_i : 0) ==
  0 && n.to_i.to_s.length % 10 == 1 && n.to_i.to_s.length % 100 != 11) || ((t = n.to_s.split(".")).count
  > 1 ? t.last.gsub(/0+$/, "").to_i : 0) != 0) ? :one : :other } } } } }'
