---
:ro: '{ :''ro'' => { :i18n => { :plural => { :keys => [:one, :few, :other], :rule
  => lambda { |n| (n.to_i.to_s.length == 1 && ((v = n.to_s.split(".")).count > 1 ?
  v.last.length : 0) == 0) ? :one : (((v = n.to_s.split(".")).count > 1 ? v.last.length
  : 0) != 0 || n.to_f == 0 || (n.to_f != 1 && (((n.to_f % 100) % 1).zero? && (1..19).include?(n.to_f
  % 100)))) ? :few : :other } } } } }'
